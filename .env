# Project Configuration
PROJECT_NAME="NewCo App"
BACKEND_CORS_ORIGINS=["http://localhost:3000"]
ENVIRONMENT=local
DOCKER_IMAGE_BACKEND=backend
TAG=latest

# Email Configuration
# For production, you'll need to configure these with your actual SMTP server
# For development, you can use services like Gmail, SendGrid, or MailHog

# Example Gmail Configuration (you'll need to enable "App Passwords" in Gmail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_TLS=true
# SMTP_SSL=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAILS_FROM_EMAIL=your-email@gmail.com
# EMAILS_FROM_NAME="NewCo App"

# Example SendGrid Configuration
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_TLS=true
# SMTP_SSL=false
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# EMAILS_FROM_EMAIL=noreply@yourdomain.com
# EMAILS_FROM_NAME="NewCo App"

# For development - MailHog (local testing SMTP server)
# Install MailHog: brew install mailhog
# Run: mailhog -smtp-bind-addr 127.0.0.1:1026 -ui-bind-addr 127.0.0.1:8026
SMTP_HOST=localhost
SMTP_PORT=1026
SMTP_TLS=false
SMTP_SSL=false
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=noreply@example.com
EMAILS_FROM_NAME=NewCo App

# Database
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis
POSTGRES_DB=app

# Security
SECRET_KEY=changethis
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis
